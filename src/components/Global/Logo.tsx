import React from "react";

type LogoProps = {};

function Logo({}: LogoProps) {
	return (
		<div className="font-bold text-xl">
			Imperial Drive<span className="text-yellow-600">.</span>
		</div>
	);
}

export default Logo;
